<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ScoreResultMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into SCORERESULT(
			NAME,	
			NUMBER,	
			SCORE,	
			START_TIME,	
			END_TIME,	
			TOTAL_TIME,	
			SC_ID
		) values (
			#{NAME},	
			#{NUMBER},	
			#{SCORE},	
			#{START_TIME},	
			#{END_TIME},	
			#{TOTAL_TIME},	
			#{SC_ID}
		)
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from SCORERESULT
		where 
			SC_ID = #{SC_ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  SCORERESULT
			set 
				NAME = #{NAME},
				NUMBER = #{NUMBER},
				SCORE = #{SCORE},
				START_TIME = #{START_TIME},
				END_TIME = #{END_TIME},
				TOTAL_TIME = #{TOTAL_TIME},
			SC_ID = SC_ID
			where 
				SC_ID = #{SC_ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			NAME,	
			NUMBER,	
			SCORE,	
			START_TIME,	
			END_TIME,	
			TOTAL_TIME,	
			SC_ID
		from 
			SCORERESULT
		where 
			SC_ID = #{SC_ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.NAME,
				a.NUMBER,
				a.SCORE,
				a.START_TIME,
				a.END_TIME,
				a.TOTAL_TIME,
				a.SC_ID
		from
				SCORERESULT a ORDER BY SCORE DESC,TOTAL_TIME
	</select>


	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.NAME,	
				a.NUMBER,	
				a.SCORE,	
				a.START_TIME,	
				a.END_TIME,	
				a.TOTAL_TIME,	
				a.SC_ID
		from 
				SCORERESULT a
	</select>


	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from SCORERESULT
		where
		SC_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
</mapper>