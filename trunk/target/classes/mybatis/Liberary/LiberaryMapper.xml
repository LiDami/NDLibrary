<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="LiberaryMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into LIBERARY(
			LIB_TITLE,	
			LIB_CON,	
			LIB_VIDEO,	
			ID
		) values (
			#{LIB_TITLE},	
			#{LIB_CON},	
			#{LIB_VIDEO},	
			#{ID}
		)
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from LIBERARY
		where 
			ID = #{ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  LIBERARY
			set 
				LIB_TITLE = #{LIB_TITLE},
				LIB_CON = #{LIB_CON},
				LIB_VIDEO = #{LIB_VIDEO},
			ID = ID
			where 
				ID = #{ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			LIB_TITLE,	
			LIB_CON,	
			LIB_VIDEO,	
			ID
		from 
			LIBERARY
		where 
			ID = #{ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.LIB_TITLE,	
				a.LIB_CON,	
				a.LIB_VIDEO,	
				a.ID
		from 
				LIBERARY a
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.LIB_TITLE,	
				a.LIB_CON,	
				a.LIB_VIDEO,	
				a.ID
		from 
				LIBERARY a
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from LIBERARY
		where
		ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
</mapper>