<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="EnterKnoMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into ENTERKNO(
			ENT_TITLE,	
			ENT_CON,	
			ENT_VIDEO,	
			ID
		) values (
			#{ENT_TITLE},	
			#{ENT_CON},	
			#{ENT_VIDEO},	
			#{ID}
		)
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from ENTERKNO
		where 
			ID = #{ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  ENTERKNO
			set 
				ENT_TITLE = #{ENT_TITLE},
				ENT_CON = #{ENT_CON},
				ENT_VIDEO = #{ENT_VIDEO},
			ID = ID
			where 
				ID = #{ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			ENT_TITLE,	
			ENT_CON,	
			ENT_VIDEO,	
			ID
		from 
			ENTERKNO
		where 
			ID = #{ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.ENT_TITLE,	
				a.ENT_CON,	
				a.ENT_VIDEO,	
				a.ID
		from 
				ENTERKNO a
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.ENT_TITLE,	
				a.ENT_CON,	
				a.ENT_VIDEO,	
				a.ID
		from 
				ENTERKNO a
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from ENTERKNO
		where
		ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
</mapper>