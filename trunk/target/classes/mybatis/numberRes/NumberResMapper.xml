<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="NumberResMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into NUMBERRES(
			NUM_NAME,	
			NUM_CON,	
			NUM_VIDEO,	
			NUM_ID
		) values (
			#{NUM_NAME},	
			#{NUM_CON},	
			#{NUM_VIDEO},	
			#{NUM_ID}
		)
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from NUMBERRES
		where 
			NUM_ID = #{NUM_ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  NUMBERRES
			set 
				NUM_NAME = #{NUM_NAME},
				NUM_CON = #{NUM_CON},
				NUM_VIDEO = #{NUM_VIDEO},
				NUM_ID = #{NUM_ID},
			NUM_ID = NUM_ID
			where 
				NUM_ID = #{NUM_ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			NUM_NAME,	
			NUM_CON,	
			NUM_VIDEO,	
			NUM_ID
		from
			NUMBERRES
		where 
			NUM_ID = #{NUM_ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.NUM_NAME,	
				a.NUM_CON,	
				a.NUM_VIDEO,	
				a.NUM_ID
		from
				NUMBERRES a
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.NUM_NAME,	
				a.NUM_CON,	
				a.NUM_VIDEO,	
				a.NUM_ID
		from
				NUMBERRES a
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from NUMBERRES
		where
		NUM_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
</mapper>